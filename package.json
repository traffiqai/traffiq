{
  "name": "traffiq",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --workspaces",
    "deploy": "sst deploy",
    "deploy:dev": "sst deploy --stage dev",
    "deploy:prod": "sst deploy --stage production",
    "start": "rm -rf .sst && cd packages/dashboard && rm -rf .next && cd ../landing && rm -rf .next && cd ../.. && sst dev --stage dev",
    "start:prod": "sst dev --stage production",
    "sst": "sst"
  },
  "workspaces": [
    "packages/*"
  ],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/aws-lambda": "8.10.157",
    "@types/node": "^24.10.0",
    "sst": "^3.9.28",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "sst": "3.17.21",
    "tailwindcss": "^3.4.3"
  }
}
